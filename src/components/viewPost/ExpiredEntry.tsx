import React, { useState} from 'react';

const ExpiredEntry : React.FC = () => {

  return(
    <React.Fragment>
      <h1> The time to reply has ended. This post no longer accepting any new entry.</h1>
    </React.Fragment>
  )
}

export default ExpiredEntry;
