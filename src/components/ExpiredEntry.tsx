import React, { useState} from 'react';
import { API, graphqlOperation } from 'aws-amplify'

type EntrySubmittedProps = {
  pid?: string;
}
const EntrySubmitted : React.FC<EntrySubmittedProps> = ({ pid }) => {


  return(
    <React.Fragment>
    </React.Fragment>
  )
}

export default EntrySubmittedProps;
